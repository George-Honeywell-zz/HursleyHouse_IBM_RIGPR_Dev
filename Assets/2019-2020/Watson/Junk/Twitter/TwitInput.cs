using System.Collections;
using System.Collections.Generic;
using System.IO;
using UnityEngine;
using Twity.DataModels.Responses;

public class TwitInput : MonoBehaviour
{
    //Dictionary<string, string> parameters = new Dictionary<string, string>();

    //void Search(string Subject, int Responses)
    //{
    //    parameters["q"] = Subject;
    //    parameters["count"] = Responses.ToString();
    //    StartCoroutine(Twity.TwitSetup.Get("search/tweets", parameters, Callback));
    //}
    //void Callback(bool success, string response)
    //{
    //    Debug.Log(response);
    //    if (success)
    //    {
    //        Debug.Log(response);
    //        SearchTweetsResponse Response = JsonUtility.FromJson<SearchTweetsResponse>(response);
    //        for (int r = 0; r < Response.statuses.Length; r++)
    //        {
    //            Debug.Log(Response.statuses[r].user.name);
    //            Debug.Log(Response.statuses[r].text);
    //        }


    //    }


    //}
}
